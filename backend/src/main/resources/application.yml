spring:
  application:
    name: graphql-checkmate-backend
  graphql:
    graphiql:
      enabled: true
      path: /graphiql

server:
  port: 8080

supabase:
  url: ${SUPABASE_URL:http://localhost:54321}
  key: ${SUPABASE_ANON_KEY:your-anon-key-here}
